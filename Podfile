# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LiveMatch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
    pod 'SDWebImage'
    pod 'LeagueAPI'
    pod 'SwiftLint'

  # Pods for LiveMatch

  target 'LiveMatchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LiveMatchUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
